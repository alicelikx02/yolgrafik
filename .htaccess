RewriteEngine On

# Eğer gerçek bir dosya veya klasör varsa onu aç
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Diğer tüm istekleri index.html'e yönlendir (React Router için)
RewriteRule ^ index.html [L]